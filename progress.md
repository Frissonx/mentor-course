День 6 (часть 1): коллбеки, анонимные и стрелочные функции — взято
- понял: функция = значение, можно передавать в другую функцию
- передать функцию (без скобок) vs вызвать (со скобками)
- техника разбора условий: что дано → что нужно → первый шаг
  Осталось на День 6: сами методы массивов (map, filter, forEach...)
- forEach + map — сделано (filter/find/цепочки/мини-проект — на завтра)
- forEach: для побочных действий, новый массив не создаёт
- map: преобразует каждый элемент, возвращает новый массив
- УРОК: тихий баг (Boolean(n%2) дал перевёрнутый результат) — всегда сверять вывод с ожиданием
filter, find, findIndex, цепочки — ВЗЯТО
- filter/find/findIndex = коллбек с условием true/false; разница в возврате (массив / 1 элемент / индекс)
- find не нашёл → undefined; findIndex не нашёл → -1
- truthy/falsy: filter(Boolean) выкидывает 0,'',null,undefined,NaN,false
- ЦЕПОЧКИ: каждый метод работает с результатом предыдущего, не с исходным массивом
- порядок: сначала filter (объекты целы), потом map (преобразуем)

День 6 ПОЛНОСТЬЮ ЗАКРЫТ: forEach, map, filter, find, findIndex, цепочки, мини-проект
- собрал 6 функций обработки users (filter/map/find комбинации)
- средняя зарплата: filter→map→forEach-сумма→деление (это ручной reduce!)
- уникальные значения: [...new Set(array)]
- УРОК: filter вернул [объект] вместо объекта — для одного элемента нужен find
  Завтра День 7: reduce

День 7: reduce
- reduce(callback(acc, current), начальноеЗначение) — acc и current это параметры ОДНОГО коллбека, не разные аргументы reduce
- то, что вернул из коллбека → станет acc на следующем шаге (сердце reduce)
- начальное значение: 0 для суммы, 1 для произведения, '' для строк — рычаг под задачу
- ОСОЗНАННО опустить начальное → reduce стартует с первого элемента (нужно для max/min/склейки)
- День 7 ЗАКРЫТ: reduce полностью, включая reduce в объект
- reduce в число (сумма, произведение, max), в строку (склейка), в ОБЪЕКТ (счётчик/группировка)
- объектный reduce: acc[ключ] = (acc[ключ] || 0) + 1; return acc; начальное {}
- челлендж Дня 6 закрыт: filter→map→reduce = активные по городам
- УРОК подтверждён: комментарий ВРАЛ (Moscow:1), реальный вывод Moscow:2 — доверяй коду, не комментарию
- выбор пути: бери тот, что разбивается на знакомые шаги, а не "всё в одном reduce"
- Завтра проверить знания reduce!!
- День 8: проверка reduce — ПРОЙДЕНА уверенно, сомнения сняты
- сам применил тернарник в reduce (длинное слово)
- сам применил SPREAD в reduce: {...acc, [key]: ...} — иммутабельный стиль, это паттерн React!
- объект с двумя полями (sum/count) за один проход
- задачу решил двумя путями (filter→reduce и filter→map→reduce) — вижу варианты
- нюанс: spread в reduce медленнее на больших данных (копирует объект), но читаемее — оба валидны