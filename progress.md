День 6 (часть 1): коллбеки, анонимные и стрелочные функции — взято

- понял: функция = значение, можно передавать в другую функцию
- передать функцию (без скобок) vs вызвать (со скобками)
- техника разбора условий: что дано → что нужно → первый шаг
  Осталось на День 6: сами методы массивов (map, filter, forEach...)
- forEach + map — сделано (filter/find/цепочки/мини-проект — на завтра)
- forEach: для побочных действий, новый массив не создаёт
- map: преобразует каждый элемент, возвращает новый массив
- УРОК: тихий баг (Boolean(n%2) дал перевёрнутый результат) — всегда сверять вывод с ожиданием
  filter, find, findIndex, цепочки — ВЗЯТО
- filter/find/findIndex = коллбек с условием true/false; разница в возврате (массив / 1 элемент / индекс)
- find не нашёл → undefined; findIndex не нашёл → -1
- truthy/falsy: filter(Boolean) выкидывает 0,'',null,undefined,NaN,false
- ЦЕПОЧКИ: каждый метод работает с результатом предыдущего, не с исходным массивом
- порядок: сначала filter (объекты целы), потом map (преобразуем)

День 6 ПОЛНОСТЬЮ ЗАКРЫТ: forEach, map, filter, find, findIndex, цепочки, мини-проект

- собрал 6 функций обработки users (filter/map/find комбинации)
- средняя зарплата: filter→map→forEach-сумма→деление (это ручной reduce!)
- уникальные значения: [...new Set(array)]
- УРОК: filter вернул [объект] вместо объекта — для одного элемента нужен find
  Завтра День 7: reduce

День 7: reduce

- reduce(callback(acc, current), начальноеЗначение) — acc и current это параметры ОДНОГО коллбека, не разные аргументы reduce
- то, что вернул из коллбека → станет acc на следующем шаге (сердце reduce)
- начальное значение: 0 для суммы, 1 для произведения, '' для строк — рычаг под задачу
- ОСОЗНАННО опустить начальное → reduce стартует с первого элемента (нужно для max/min/склейки)
- День 7 ЗАКРЫТ: reduce полностью, включая reduce в объект
- reduce в число (сумма, произведение, max), в строку (склейка), в ОБЪЕКТ (счётчик/группировка)
- объектный reduce: acc[ключ] = (acc[ключ] || 0) + 1; return acc; начальное {}
- челлендж Дня 6 закрыт: filter→map→reduce = активные по городам
- УРОК подтверждён: комментарий ВРАЛ (Moscow:1), реальный вывод Moscow:2 — доверяй коду, не комментарию
- выбор пути: бери тот, что разбивается на знакомые шаги, а не "всё в одном reduce"
- Завтра проверить знания reduce!!
- День 8: проверка reduce — ПРОЙДЕНА уверенно, сомнения сняты
- сам применил тернарник в reduce (длинное слово)
- сам применил SPREAD в reduce: {...acc, [key]: ...} — иммутабельный стиль, это паттерн React!
- объект с двумя полями (sum/count) за один проход
- задачу решил двумя путями (filter→reduce и filter→map→reduce) — вижу варианты
- нюанс: spread в reduce медленнее на больших данных (копирует объект), но читаемее — оба валидны
- День 8 (объекты): деструктуризация, Object.keys/values/entries
- деструктуризация: const { name, city } = user — достать поля в переменные
- деструктуризация В ПАРАМЕТРЕ: function describe({ name, age }) — это форма React-компонентов!
- доступ: точка (имя известно) vs скобки (имя в переменной)
- мост объект→массив: Object.keys (ключи) / values (значения) / entries (пары [k,v])
- → дальше можно применять map/filter/reduce. Пример: Object.values(obj).reduce(...)
- День 8 (объекты, часть 2): entries, копирование
- Object.entries → пары [ключ, значение]; деструктуризация пары: ([name, value]) — КВАДРАТНЫЕ скобки (массив!)
- {объект} деструктуризация — фигурные; [массив/пара] — квадратные
- КОПИРОВАНИЕ: const copy = original НЕ копирует (одна ссылка, меняется оригинал!)
- настоящая копия: { ...original } — новый объект (это закон React: не мутировать состояние)
- цепочка на парах: Object.entries → filter([k,v]) → map([k]) — соединил всё разом
- День 8 — самочувствие: день насыщенный, читать код легче чем писать (мало практики, норма — закроется объёмом)
- ~3-3.5ч всего, из них код ~1-1.5ч, остальное осмысление + записи (это ОК, фундамент строится так)
- ЗАВТРА: повторить объекты + мини-проект на закрепление (практика письма!)
- День 9 разминка: объекты повторил — всё всплыло уверенно
- reduce: возвращай acc + value (НЕ acc += value) — reduce сам подхватывает возвращённое, мутировать не надо
- сам применил .map(([name]) => name) — беру только нужный элемент пары
- День 9 ЗАКРЫТ: мини-проект "аналитика магазина" — 5 функций с нуля
- БАГ: забыл filter (вернул все товары вместо "в наличии") — читать условие ДО кода, слово "в наличии" = filter
- имя функции = контракт: getProductsByCategory должна вернуть товары, не названия
- УНИВЕРСАЛЬНО > ХАРДКОД: acc[product.category] лучше чем вписанные electronics/furniture руками (не сломается на новой категории)
- рефлекс: повтор (price\*sold) вынести в const revenue
- reduce: возвращать acc, не мутировать через +=
- письмо с нуля идёт медленнее чтения — это та самая мышца, качаю
- - spread-reduce (Б: {...acc}) vs мутация (А: acc[key]=...; return acc):
    Б — иммутабельно, читаемо, стиль React, но копирует объект каждый шаг → медленно на больших данных
    А — мутирует acc, быстро; ВНУТРИ reduce мутировать acc БЕЗОПАСНО (он локальный, снаружи не виден)
    выбор под задачу: React/состояние → Б; большие данные/скорость → А
- - ГИБРИД написал сам: getRevenue (мелкая функция, одна идея) + getCategoryRevenue (мой 1-проходный reduce, вызывает getRevenue внутри)
- синтез: декомпозиция коллеги + скорость моего reduce
- понял: маленькие функции вызываются внутри больших = декомпозиция (это и был "функция как значение" из дня 1)
- ВЕХА: от "что такое коллбек" до "проектирую из переиспользуемых кусков" за 9 дней
- День 9 — самонаблюдение (ВАЖНО):
- забывать имена методов/объектов — НОРМА, приходит от объёма, даже сеньоры гуглят. Не зубрить.
- читать код: разбирать построчно вслух "что за что отвечает" — превращает "интуитивно" в "точно знаю"
- ЛОВУШКА: забыл простое → мозг выдумывает сложное. Рефлекс: код разрастается = СТОП, есть способ проще. Сложно ≈ неправильно.
- разрыв чтение/письмо закрывается практикой письма (объём)
- - функцию можно ПЕРЕДАТЬ как коллбек (без скобок), если её параметры совпадают с тем, что метод даёт коллбеку
- map даёт (element, ...) → getRevenue(product) подходит → products.map(getRevenue) ✅
- reduce даёт (acc, current) → getRevenue не подходит голой → вызывать внутри: getRevenue(product) ✅
- вызвать (скобки есть) vs передать (скобок нет) — это всё тот же день 1
- - передать функцию (метод вызовет за меня, напр. map(getRevenue)) vs вызвать (getRevenue(product), нужен результат сейчас)
- в reduce getRevenue ВЫЗЫВАЮ внутри тела — нужен результат здесь и сейчас, не передаю
- голым в map можно ТОЛЬКО если нужен чистый результат функции; нужно "плюс ещё что-то" → оборачиваю в стрелку
- - ментор-2 показал ФУНКЦИЮ ВЫСШЕГО ПОРЯДКА: sumByCategory(products, func) — принимает функцию-стратегию параметром
- func ПЕРЕДаётся снаружи, ВЫЗЫВАется внутри reduce: func(product) — синтез "передать vs вызвать"
- сила: один движок группировки + сменные мини-функции (getRevenue/getDiscountedPrice/...) — не плодить копии
- это паттерн "стратегия" (загуглить потом). Общий алгоритм один, поведение подставляю функцией
- ВСЯ сегодняшняя линия сошлась: map(getRevenue) → "можно передать?" → передать vs вызвать → проектирование с функцией-параметром
- День 10: асинхронность — введение
- синхронный код = сверху вниз, мгновенно. Асинхронный = "сделать потом, не ждать"
- setTimeout(функция, задержка) — передаёшь функцию, её вызовут ПОЗЖЕ (это "передать функцию" из дня 9!)
- JS однопоточный: одна вещь за раз. Отложенное (таймеры, ответы сервера) идёт в ОЧЕРЕДЬ
- ЖЕЛЕЗНОЕ ПРАВИЛО: сначала весь синхронный код до конца, ПОТОМ очередь отложенного
- setTimeout(...,0) ≠ "сейчас", а "как можно скорее, но после текущего кода"
- механизм называется event loop (детали потом)
- число с дробью в JS — через ТОЧКУ (1.5), не запятую
- День 10 (промисы):
- Promise = объект-"обещание" результата в будущем. 3 состояния: pending → fulfilled (resolve) ИЛИ rejected (reject)
- new Promise((resolve, reject) => {...}): resolve(значение)=успех, reject(ошибка)=провал
- .then(коллбек) — когда успех; .catch(коллбек) — когда ошибка (снова "передаю функцию, вызовут позже")
- .then цепляются плоско (вместо лесенки коллбеков = callback hell)
- ПОНЯЛ: код снаружи setTimeout = сразу; внутри = отложено. Где Math.random() — там и момент решения
- День 10 (async/await) — ВЗЯТО:
- async/await = удобный синтаксис ПОВЕРХ промисов (не новый механизм)
- await = "подожди промис, дай результат в переменную"; работает ТОЛЬКО внутри async-функции
- await приостанавливает свою async-функцию, но не всю программу
- ошибки: try { await ... } catch (error) — замена .then/.catch
- вызвать функцию (fetchData()) → она вернёт промис → await его ждёт
- ЛИНИЯ ДНЯ: setTimeout (вызовут позже) → Promise (обещание + then/catch) → async/await (как синхронный код). Одно и то же, три формы
- День 10 ЗАКРЫТ: fetch — РЕАЛЬНЫЕ данные с сервера
- fetch(url) возвращает промис → await его
- ДВА await: const res = await fetch(url); const data = await res.json()
- (1) fetch ждёт ОТВЕТ сервера, (2) .json() парсит тело из JSON в JS-объект
- JSON — текстовый формат данных, выглядит как объект; .json() → настоящий объект
- СОЕДИНИЛ ВСЁ: fetch + async/await (сегодня) + slice/map (неделю назад) на реальных данных
- getPosts: запросил 100 постов с сервера → slice(0,3) → map(title) — полный цикл фронтендера!
- после await данные на руках → дальше код синхронный, сверху вниз
- День 10 — про синтаксис асинхронности (разгрузка):
- НЕ зубрить всю кучу. Ежедневно нужна ОДНА форма: async + try { await fetch → await .json() } catch
- resolve/reject (new Promise вручную) — для ПОНИМАНИЯ капота, сам почти не пишу (fetch уже даёт промис)
- .then/.catch — старый стиль, важно УЗНАВАТЬ в чужом коде, сам пишу async/await
- синтаксис въедается от ПОВТОРОВ ПИСЬМА с нуля, не от времени. План "набить в проектах" — верный
- День 11: DOM — первая живая страница
- DOM = дерево объектов страницы; HTML статичный текст → DOM живые объекты, JS их меняет
- точка входа: document
- НАЙТИ: document.querySelector('#id' / '.class' / 'tag') — первый; querySelectorAll — все (как find/filter)
- ИЗМЕНИТЬ: el.textContent = '...' (текст); el.style.color = '...' (стиль)
- СОБЫТИЯ: el.addEventListener('click', функция) — браузер вызовет функцию при клике
- ЭТО ТОТ ЖЕ ПАТТЕРН: передаю функцию, вызовут позже (setTimeout/fetch/addEventListener — один механизм, разные триггеры)
- подключение: <script src="script.js"></script> в КОНЦЕ body
- День 11 (часть 2): счётчик — состояние + интерактивность
- состояние (state): let count=0 ЖИВЁТ СНАРУЖИ обработчиков → помнит значение между кликами
- два события (клик/сброс) меняют ОДНО состояние
- ПАТТЕРН (ядро React!): состояние → событие меняет состояние → render() показывает на странице
- повтор обновления текста вынес в render() (урок дня 9: не дублировать)
- React сделает render() АВТОМАТИЧЕСКИ за меня — вот что он упрощает. Сейчас зову руками
- - hoisting: function name(){} доступна во всём файле (можно звать до объявления); стрелка в const — НЕ поднимается
- ЗАВТРА: fetch + DOM — загрузить данные с сервера и показать СПИСКОМ на странице (синтез дней 10-11)
- День 12: fetch + DOM — ПЕРВОЕ веб-приложение
- соединил 6 навыков: async/await+fetch+try/catch (д.10) + querySelector+event (д.11) + map/join (ранние)
- данные с сервера → .map(u => `<li>${u.name}</li>`).join('') → .innerHTML → список на странице
- map/join которые делал на числах — теперь рисуют HTML. Тот же инструмент, новое применение
- структура: неизменные элементы искать ОДИН раз снаружи, не внутри функции
- addEventListener('click', loadUsers) — можно голой, без () => (день 9: передать vs вызвать)
  День 13 (блок 1): чтение ввода
- input.value — текущий текст поля; событие 'input' — при каждом изменении
- БЕЗОПАСНОСТЬ: пользовательский ввод → ТОЛЬКО .textContent (как текст).
  .innerHTML рисует ввод как HTML → дыра XSS (введут <script> — выполнится!)
- .innerHTML — только для СВОЕЙ разметки из своих данных
- дефолт из страницы (не хардкод) + input.value || default (falsy-идиома)
  День 13 (блок 2): submit — ВЗЯТО (через баг и починку)
- событие 'submit' на ФОРМЕ (не кнопке); срабатывает и кнопкой, и Enter
- ПОДВОХ: form по умолчанию перезагружает страницу → event.preventDefault() ПЕРВОЙ строкой, всегда
- event — объект события (браузер передаёт в коллбек сам); это НЕ элемент страницы
- .value можно читать И присваивать: taskInput.value = '' очищает поле
- БАГИ дня (оплачены): опечатка from/form роняет код → ЧИТАТЬ КОНСОЛЬ (F12) первым делом;
  у формы нет .value — текст в ПОЛЕ; не смешивать куски разных блоков
- цикл багфикса: консоль → ошибка → причина → починка = обычная работа программиста, не провал
  День 13 (вечерний разбор кода комментариями):
- сильное: объясняю ЗАЧЕМ (defaultName, preventDefault) — это понимание роли
- плывёт 1: querySelector комментирую одинаково ("первый совпавший") — пересказ синтаксиса, не роль. Формула: "нахожу X, чтобы потом Y"
- плывёт 2: addEventListener — слепил функцию и параметр. ТОЧНО: подписка на событие; функция = обработчик (её вызовет браузер); event = объект с инфой, браузер кладёт его в параметр сам
- завтра на разогреве: проговорить addEventListener заново + Блок 3 (todo-ядро)
  День 14: ступор перед чистым листом — РАЗОБРАН техникой
- "непродуктивный день" оказался страхом чистого листа: на вопросы про понимание отвечал точно, встал на "пиши с нуля"
- ЛЕЧЕНИЕ: дано → нужно → первый шаг СЛОВАМИ В ЧАТ, не открывая редактор. Лист перестаёт быть чистым — код тянется за словами
- сам спроектировал todo словами за 3 ответа. Перевод рус→JS = куски из дней 11-13
- термины: <h1> заголовок, <p> абзац, <ul> СПИСОК (не "лист")
  День 14 ЗАКРЫТ: TODO-ЯДРО НАПИСАНО (через ступор и починку!)
- маршрут из ступора: 3 вопроса словами → каркас → 5 мелких правок → работает. "Сломался" = мелочи слиплись
- структура файла: состояние → элементы (1 раз снаружи) → render → обработчик
- паттерн в чистом виде: tasks[] → submit пушит → render рисует (map+join+innerHTML)
- if (!input.value) return — ранний выход на falsy
- ОПЕЧАТКА "use strict" пережила ДВЕ правки (strick→stricе, русская е!) — набирать заново, не править
- submit вешается на ФОРМУ; кнопка type=submit не нуждается в id и обработчике
  День 14 — итог честного учёта:

* решил кризис семьи за день (подработка → аванс → очки)
* написал todo-приложение, сам нашёл и закрыл XSS
* кран = страховочная сетка, которая ПОЗВОЛЯЕТ учиться без страха, а не откат
  − три опечатки за день → внимательность в плохие дни ниже, учитывать контекст
  ПРАВИЛО: самокритика остаётся + одна строка "что сделано хорошо" КАЖДЫЙ день. Это калибровка, не расслабление

- ментор-C# против раннего React. Его правда: слабый JS = провал на собесах. МОЙ ответ: глубина JS — параллельно, ПО ЗАПРОСУ практики + блок теории перед собесами (мес 5-6)
- его метод (глубина до практики) УЖЕ не сработал на мне с C# — несовместим с моими условиями (6 мес, ипотека, вечера)
- моя стратегия легальна: спиральное обучение — рабочий уровень → применение → углубление когда есть "зачем"
- чек-лист React-готовности ЗАКРЫТ: методы массивов+reduce, деструктуризация (пропсы!), spread/иммутабельность, fetch, паттерн состояние→событие→render РУКАМИ
- 3 ментора = слушай всех, решай сам по СВОИМ условиям
  День 15: разогрев после 3-дневной паузы — СТРАХ "всё забыл" НЕ подтвердился
- блок 1 (filter/map/reduce/find) — всё с ходу; блок 2 (объекты) — с ходу; async-каркас — верный
- деструктуризацию не "забыл", а пропустил: const { name, age } = users[0] — это тот же user.name другим синтаксисом
- опечатки: .json() БЕЗ аргумента; не забыть console.log(result) — данные пришли, показать забыл
- ВЫВОД: понятое руками паузу переживает. "Забыл" = не разогрелся, лечится за 5 минут. Не паниковать на будущее
  TODO ЗАВЕРШЁН: добавление + удаление + счётчик
- удаление: кнопка создаётся в render, addEventListener вешается ПРИ СОЗДАНИИ (решение "клик по кнопке, которой ещё нет")
- splice(index, 1) удаляет по индексу (МУТИРУЕТ массив, в отличие от filter/map); надёжно т.к. render перерисовывает всё
- счётчик в render через tasks.length — обновляется и при add, и при delete автоматически (сила паттерна состояние→render)
- на потом: в React удаление через filter (новый массив) — чище. splice-by-index = развилка
- сборка элемента: сначала li.append(кнопку), потом list.append(li) — "собрал, потом повесил"
  День 16: React СТАРТ
- проект через Vite (npm create vite → React → JS → npm install → npm run dev → localhost:5173)
- src/App.jsx — главный компонент
- компонент = функция с большой буквы, return JSX (разметка в JS), export default
- HMR: сохранил файл → страница сама обновилась (первое "волшебство")
- src — сердце проекта, тут код
  День 16: JSX + данные
- {} в JSX = вставить JS-ВЫРАЖЕНИЕ (не текст): {name}, {year+5}, {year+2\*6}→2038 (React вычисляет, с приоритетом операций)
- это как ${} в шаблонных строках, но в JSX без доллара
- компонент возвращает ОДИН корневой элемент → всё оборачиваем в <div>
- многострочный JSX оборачиваем в return ( ... )
  День 16: компоненты + пропсы
- компонент используется как тег: <UserCard name="Анна" age={28} />
- props = данные снаружи; деструктуризация в параметре: function UserCard({ name, age }) — форма из дня 8!
- один компонент + разные пропсы = разные результаты (переиспользуемость, как функции-стратегии)
- передача: текст в кавычках (name="Анна"), число/JS в фигурных ({age={28}})
  День 16 КУЛЬМИНАЦИЯ: useState — ручной render УМЕР
- import { useState } from "react"
- const [count, setCount] = useState(0) — деструктуризация массива: count (читать), setCount (менять), 0 (начальное)
- onClick={() => setCount(count + 1)} — обработчик в JSX
- setCount(...) → React САМ перерисовывает компонент. ВОТ КУДА ДЕЛСЯ render() из дня 11
- состояние меняют ТОЛЬКО через сеттер (setCount), НЕ напрямую (count=5 не сработает) — иммутабельность из дня 8 как закон
- сравнение: день 11 = count++; render() вручную. React = setCount(), перерисовка сама
  День 16 (утро) ЗАКРЫТ: всё ядро React за сессию
- компоненты + JSX {} + пропсы (деструктуризация) + useState (реактивное состояние)
- ручной render из дня 11 умер — React перерисовывает сам через setCount
- ВЕХА: React лёг на понятое руками, не как магия. "Рано/не рано" — не рано, доказано
- ПЛАН после перерыва: разминка по сегодняшнему → рендеринг списков (.map рисует компоненты) → todo на React
  День 16 (вечер) разминка: компоненты+пропсы+useState — осело, воспроизвёл сам после перерыва
  День 16: рендеринг списков
- {array.map(item => <Component key={item.id} prop={item.field} />)} — map рисует КОМПОНЕНТЫ, не строки; join не нужен
- key={уникальный} обязателен (обычно id) — React отличает элементы для умной перерисовки
- это тот же .map() из методов массивов, только возвращает JSX
- ловушка: достать ПОЛЕ из объекта (product.title), не весь объект; похожие строки (title=/price=) — проверять каждую
